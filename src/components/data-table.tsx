import { EmptyState } from "@/components/empty-state";
export function DataTable({ headers, rows }: { headers: string[]; rows: React.ReactNode[][] }) { if (!rows.length) return <EmptyState/>; return <div className="overflow-hidden rounded-2xl border bg-[var(--card)]"><div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left text-sm"><thead className="bg-[var(--muted)]"><tr>{headers.map(h=><th key={h} className="px-4 py-3 font-bold">{h}</th>)}</tr></thead><tbody className="divide-y">{rows.map((row,i)=><tr key={i} className="hover:bg-[var(--muted)]/50">{row.map((cell,j)=><td key={j} className="px-4 py-4">{cell}</td>)}</tr>)}</tbody></table></div></div>; }
