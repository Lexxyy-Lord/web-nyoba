"use client";
import { useState } from "react";
import { toast } from "sonner";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
export function AdminPricingForm(){const [cost,setCost]=useState(2500);const [fixed,setFixed]=useState(10000);const [percent,setPercent]=useState(0);const total=Math.ceil((cost+fixed+cost*percent/100)/100)*100;async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const payload={...Object.fromEntries(f.entries()),active:true};const r=await fetch("/api/admin/pricing-rules",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});const b=await r.json();if(!r.ok||!b.success)return toast.error(b.error?.message??"Gagal");toast.success("Aturan harga tersimpan")}return <form onSubmit={submit} className="space-y-5"><div className="grid gap-4 md:grid-cols-3"><div><Label>Nama aturan</Label><Input name="name" defaultValue="Keuntungan global" required/></div><div><Label>Scope</Label><select name="scope" className="h-11 w-full rounded-xl border bg-[var(--card)] px-3"><option>GLOBAL</option><option>SERVICE</option><option>COUNTRY</option><option>PROVIDER</option></select></div><div><Label>Tipe keuntungan</Label><select name="profitType" className="h-11 w-full rounded-xl border bg-[var(--card)] px-3"><option>FIXED</option><option>PERCENTAGE</option><option>COMBINED</option></select></div><div><Label>Nominal tetap</Label><Input name="fixedAmount" value={fixed} onChange={e=>setFixed(Number(e.target.value))}/></div><div><Label>Persentase</Label><Input name="percentage" value={percent} onChange={e=>setPercent(Number(e.target.value))}/></div><div><Label>Pembulatan</Label><select name="roundingIncrement" className="h-11 w-full rounded-xl border bg-[var(--card)] px-3"><option value="100">Rp100</option><option value="500">Rp500</option><option value="1000">Rp1.000</option></select></div><Input type="hidden" name="priority" value="0"/><Input type="hidden" name="minimumSelling" value="0"/><div className="rounded-2xl bg-[var(--muted)] p-5 md:col-span-3"><p className="text-sm font-bold">Preview</p><div className="mt-3 grid gap-3 sm:grid-cols-3"><label>Harga API<Input value={cost} onChange={e=>setCost(Number(e.target.value))}/></label><div><p className="text-xs text-[var(--muted-foreground)]">Keuntungan</p><p className="mt-2 text-xl font-black">Rp{(total-cost).toLocaleString("id-ID")}</p></div><div><p className="text-xs text-[var(--muted-foreground)]">Harga user</p><p className="mt-2 text-xl font-black">Rp{total.toLocaleString("id-ID")}</p></div></div></div></div><Button>Simpan aturan</Button></form>}
