import{prisma}from"@/lib/db";import{formatRupiah}from"@/lib/money";import{PageHeader}from"@/components/page-header";import{DataTable}from"@/components/data-table";import{StatusBadge}from"@/components/status-badge";export default async function Page(){const rows=await prisma.deposit.findMany({include:{user:true},take:200,orderBy:{createdAt:"desc"}});return <><PageHeader title="Deposit Manual" description="Permintaan diarahkan ke WhatsApp super admin. Persetujuan hanya boleh dilakukan setelah pembayaran diverifikasi."/><DataTable headers={["ID","User","Nominal","Metode","Status","Dibuat"]} rows={rows.map(d=>[d.internalDepositNumber,d.user.username,formatRupiah(d.amountReceived),d.method,<StatusBadge key={d.id} status={d.status}/>,d.createdAt.toLocaleString("id-ID",{timeZone:"Asia/Jakarta"})])}/></>}
