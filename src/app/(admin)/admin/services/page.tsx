import{prisma}from"@/lib/db";import{PageHeader}from"@/components/page-header";import{DataTable}from"@/components/data-table";export default async function Page(){const rows=await prisma.otpService.findMany({include:{_count:{select:{countries:true,orders:true}}},orderBy:{name:"asc"}});return <><PageHeader title="Layanan" description="Cache dan sinkronisasi daftar aplikasi RumahOTP."/><DataTable headers={["Kode","Nama","Aktif","Negara","Order","Sinkron terakhir"]} rows={rows.map(s=>[String(s.serviceCode),s.name,s.active?"Ya":"Tidak",String(s._count.countries),String(s._count.orders),s.syncedAt?.toLocaleString("id-ID",{timeZone:"Asia/Jakarta"})??"Belum"])} /></>}
