import{prisma}from"@/lib/db";import{formatRupiah}from"@/lib/money";import{PageHeader}from"@/components/page-header";import{DataTable}from"@/components/data-table";import{StatusBadge}from"@/components/status-badge";export default async function Page(){const rows=await prisma.otpOrder.findMany({include:{user:true},take:300,orderBy:{createdAt:"desc"}});return <><PageHeader title="Pesanan OTP" description="Harga modal dan keuntungan hanya tersedia untuk admin."/><DataTable headers={["Internal","Provider","User","Layanan","Negara","Nomor","Modal","Jual","Profit","Status"]} rows={rows.map(o=>[o.internalOrderNumber,o.providerOrderId??"-",o.user.username,o.serviceName,o.countryName,o.phoneNumber??"-",formatRupiah(o.costPrice),formatRupiah(o.sellingPrice),formatRupiah(o.profitAmount),<StatusBadge key={o.id} status={o.status}/>])}/></>}
