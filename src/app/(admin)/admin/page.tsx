import{Users,Wallet,ReceiptText,TrendingUp,CheckCircle2,XCircle,Clock3,Server}from"lucide-react";import{prisma}from"@/lib/db";import{rumahOtp}from"@/lib/rumahotp/services";import{formatRupiah}from"@/lib/money";import{PageHeader}from"@/components/page-header";import{StatCard}from"@/components/stat-card";import{SectionCard}from"@/components/section-card";import{DataTable}from"@/components/data-table";import{StatusBadge}from"@/components/status-badge";export default async function Page(){const [users,balance,orders,success,failed,pending,totals,latest,provider]=await Promise.all([prisma.user.count(),prisma.userBalance.aggregate({_sum:{balance:true}}),prisma.otpOrder.count(),prisma.otpOrder.count({where:{status:{in:["OTP_RECEIVED","COMPLETED"]}}}),prisma.otpOrder.count({where:{status:"FAILED"}}),prisma.deposit.count({where:{status:{in:["REQUESTED","WAITING_PAYMENT","MANUAL_REVIEW"]}}}),prisma.otpOrder.aggregate({_sum:{sellingPrice:true,costPrice:true,profitAmount:true}}),prisma.otpOrder.findMany({take:8,orderBy:{createdAt:"desc"},include:{user:true}}),rumahOtp.getBalance().catch(()=>null)]);return <><PageHeader title="Dashboard Admin" description="Kinerja bisnis, status provider, dan aktivitas terbaru."/><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><StatCard title="Total pengguna" value={String(users)} icon={Users}/><StatCard title="Total saldo user" value={formatRupiah(balance._sum.balance??0n)} icon={Wallet}/><StatCard title="Total omzet" value={formatRupiah(totals._sum.sellingPrice??0n)} icon={ReceiptText}/><StatCard title="Total keuntungan" value={formatRupiah(totals._sum.profitAmount??0n)} icon={TrendingUp}/><StatCard title="Pesanan berhasil" value={String(success)} icon={CheckCircle2}/><StatCard title="Pesanan gagal" value={String(failed)} icon={XCircle}/><StatCard title="Deposit pending" value={String(pending)} icon={Clock3}/><StatCard title="Saldo RumahOTP" value={provider?formatRupiah(provider.balance):"Tidak tersedia"} icon={Server}/></div><div className="mt-6"><SectionCard title="Transaksi terbaru"><DataTable headers={["Order","User","Layanan","Modal","Jual","Profit","Status"]} rows={latest.map(o=>[o.internalOrderNumber,o.user.username,o.serviceName,formatRupiah(o.costPrice),formatRupiah(o.sellingPrice),formatRupiah(o.profitAmount),<StatusBadge key={o.id} status={o.status}/>])}/></SectionCard></div></>}
