import{prisma}from"@/lib/db";import{formatRupiah}from"@/lib/money";import{PageHeader}from"@/components/page-header";import{DataTable}from"@/components/data-table";export default async function Page(){const rows=await prisma.user.findMany({include:{role:true,balance:true,_count:{select:{otpOrders:true}}},take:200,orderBy:{createdAt:"desc"}});return <><PageHeader title="Manajemen Pengguna" description="Cari, audit, aktifkan, blokir, dan kelola role pengguna melalui API admin."/><DataTable headers={["ID","Nama","Username","Email","Role","Status","Saldo","Order"]} rows={rows.map(u=>[u.id,u.name,u.username,u.email,u.role.key,u.status,formatRupiah(u.balance?.balance??0n),String(u._count.otpOrders)])}/></>}
