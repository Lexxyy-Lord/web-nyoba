import { AppShell } from "@/components/app-shell";import{adminNavigation}from"@/data/navigation";import{requireAdminPage}from"@/lib/auth/guards";export const dynamic="force-dynamic";export default async function Layout({children}:{children:React.ReactNode}){const u=await requireAdminPage();return <AppShell admin items={adminNavigation} user={{name:u.name,email:u.email,role:u.role.key}}>{children}</AppShell>}
