import{prisma}from"@/lib/db";import{formatRupiah}from"@/lib/money";import{PageHeader}from"@/components/page-header";import{DataTable}from"@/components/data-table";export default async function Page(){const rows=await prisma.otpProvider.findMany({include:{country:{include:{service:true}},_count:{select:{operators:true}}},take:300,orderBy:{updatedAt:"desc"}});return <><PageHeader title="Negara dan Provider" description="Harga, stok, dan identifier internal hanya terlihat oleh admin."/><DataTable headers={["Layanan","Negara","Provider","Stok","Modal","Operator","Tersedia"]} rows={rows.map(p=>[p.country.service.name,p.country.name,p.providerExternalId,String(p.stock),formatRupiah(p.costPrice),String(p._count.operators),p.available?"Ya":"Tidak"])} /></>}
