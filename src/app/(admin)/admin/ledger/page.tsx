import{prisma}from"@/lib/db";import{formatRupiah}from"@/lib/money";import{PageHeader}from"@/components/page-header";import{DataTable}from"@/components/data-table";export default async function Page(){const rows=await prisma.balanceLedger.findMany({include:{user:true},take:500,orderBy:{createdAt:"desc"}});return <><PageHeader title="Mutasi Saldo" description="Ledger immutable untuk deposit, pembelian, refund, koreksi, bonus, dan tindakan admin."/><DataTable headers={["Transaksi","User","Jenis","Debit","Kredit","Sebelum","Sesudah","Keterangan"]} rows={rows.map(l=>[l.transactionId,l.user.username,l.type,formatRupiah(l.debit),formatRupiah(l.credit),formatRupiah(l.balanceBefore),formatRupiah(l.balanceAfter),l.description])}/></>}
