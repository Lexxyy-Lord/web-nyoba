import{prisma}from"@/lib/db";import{PageHeader}from"@/components/page-header";import{DataTable}from"@/components/data-table";export default async function Page(){const rows=await prisma.adminActivityLog.findMany({include:{admin:true},take:500,orderBy:{createdAt:"desc"}});return <><PageHeader title="Log Aktivitas" description="Audit tindakan sensitif admin dengan IP, user agent, dan perubahan data."/><DataTable headers={["Admin","Aksi","Entitas","Deskripsi","IP","Waktu"]} rows={rows.map(l=>[l.admin.username,l.action,`${l.entityType}:${l.entityId??"-"}`,l.description,l.ipAddress??"-",l.createdAt.toLocaleString("id-ID",{timeZone:"Asia/Jakarta"})])}/></>}
