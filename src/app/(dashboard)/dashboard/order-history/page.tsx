import Link from "next/link";import {requireUser} from "@/lib/auth/guards";import{prisma}from"@/lib/db";import{formatRupiah}from"@/lib/money";import{PageHeader}from"@/components/page-header";import{DataTable}from"@/components/data-table";import{StatusBadge}from"@/components/status-badge";export default async function Page(){const u=await requireUser();const rows=await prisma.otpOrder.findMany({where:{userId:u.id},orderBy:{createdAt:"desc"},take:100});return <><PageHeader title="Riwayat Pesanan" description="Harga modal dan keuntungan disembunyikan untuk pengguna biasa."/><DataTable headers={["Order","Layanan","Negara","Nomor","Harga","Status","Tanggal"]} rows={rows.map(o=>[<Link key={o.id} href={`/dashboard/orders/${o.id}`} className="font-bold text-indigo-500">{o.internalOrderNumber}</Link>,o.serviceName,o.countryName,o.phoneNumber??"-",formatRupiah(o.sellingPrice),<StatusBadge key={`${o.id}s`} status={o.status}/>,new Intl.DateTimeFormat("id-ID",{dateStyle:"medium",timeZone:"Asia/Jakarta"}).format(o.createdAt)])}/></>}
