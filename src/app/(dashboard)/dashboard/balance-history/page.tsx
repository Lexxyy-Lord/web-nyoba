import{requireUser}from"@/lib/auth/guards";import{prisma}from"@/lib/db";import{formatRupiah}from"@/lib/money";import{PageHeader}from"@/components/page-header";import{DataTable}from"@/components/data-table";export default async function Page(){const u=await requireUser();const rows=await prisma.balanceLedger.findMany({where:{userId:u.id},orderBy:{createdAt:"desc"},take:200});return <><PageHeader title="Riwayat Saldo" description="Setiap perubahan saldo tersimpan sebagai ledger yang tidak dapat diubah dari browser."/><DataTable headers={["ID Transaksi","Jenis","Keterangan","Debit","Kredit","Saldo Akhir","Tanggal"]} rows={rows.map(l=>[l.transactionId,l.type,l.description,formatRupiah(l.debit),formatRupiah(l.credit),formatRupiah(l.balanceAfter),new Intl.DateTimeFormat("id-ID",{dateStyle:"medium",timeStyle:"short",timeZone:"Asia/Jakarta"}).format(l.createdAt)])}/></>}
